import {ReplaySession} from "./replay/session"; import {QuantPipeline} from "./quant/pipeline";
export function seededBars(symbol:string,n=240,seed=17){const profiles:Record<string,[number,number,number]>={SPX:[6481.2,.0001,.01],NDX:[23914.6,.00016,.012],BTC:[113420,.00022,.02],GOLD:[3812.4,.00008,.01],BRENT:[68.7,-.00002,.016],EURUSD:[1.174,.00003,.006]};const [base,drift,vol]=profiles[symbol]??[100,.0001,.012];let x=(seed+symbol.length*97)>>>0,p=base;const bars=[];for(let i=0;i<n;i++){x=(x*1664525+1013904223)>>>0;const shock=drift+(x/4294967296-.5)*vol;p*=1+shock;const open=i?p*(1-shock):p/(1+shock),spread=Math.max(.001,Math.abs(shock)*.8+vol*.2);bars.push({time:Date.now()-(n-i)*300000,open,high:Math.max(open,p)*(1+spread),low:Math.min(open,p)*(1-spread),close:p,volume:10000+(x%60000)})}return bars}
export function runReplay(symbol:string,cursor=240){const session=ReplaySession.fromBars(seededBars(symbol),symbol),pipe=new QuantPipeline();session.replay(e=>pipe.onBar(e),undefined,session.events[Math.min(cursor,session.events.length)-1]?.timestamp);return{session,snapshot:pipe.snapshot()}}
